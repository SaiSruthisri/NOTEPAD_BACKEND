<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Website</title>
    <link rel="stylesheet"  href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    
</head>
<body>
    <div class = "w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class ="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block"href="/logout">Logout</a>
        </div>
        
        
        <h5 class ="mb-5">edit your post.</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea placeholder="What's on your mind?"class ="block p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name ="content"><%= post.content %></textarea>
            <input class ="px-3 py-2 w-40 text-black-sm bg-yellow-500  block rounded-md mt-2" type ="submit" value="Edit post"> 
        </form>
        
     
    </div>
</body>
</html>  -->



<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Website</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-between items-center w-full ">
            <div class="flex items-center gap-3 ">
                <a class="text-blue-500 inline-block mb-10 whitespace-nowrap" href="/profile">Go Back to Profile</a>
            </div>
            <div class="w-full flex justify-end">
                <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>
            </div>
        </div>

        <h5 class="mb-5">Edit Your Post</h5>
        <form method="post" action="/update/<%= post._id %>">
            <!-- Title Input (Make it Wider) -->
            <input type="text" name="title" placeholder="Enter title..."
                class="block p-3 outline-none w-full sm:w-2/3 bg-transparent border-2 border-zinc-800 rounded-md mb-3"
                value="<%= post.title %>">
        
            <!-- Content Textarea (Make it Large & Freestyle) -->
            <textarea name="content" placeholder="Write your content here..."
                class="block p-3 outline-none w-full sm:w-2/3 h-60 resize-y bg-transparent border-2 border-zinc-800 rounded-md"><%= post.content %></textarea>
        
            <!-- Submit Button -->
            <input class="px-5 py-3 w-40 text-black-sm bg-yellow-500 block rounded-md mt-5" type="submit"
                value="Edit Post">
        </form>
        
    </div>
</body>

</html>